<h1> All Characters </h1>
<%= link_to "Add Character", new_character_path %>


<% @characters.each do |char| %>
    <div>
        <h2>
            Name: <%= link_to char.name, char %>
            </br>
            House: <%= link_to char.house.name, char.house %>
        </h2>
        <%= image_tag char.img_url %>
    </div>
<% end %>
